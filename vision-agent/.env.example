# Stream (transport) — reuse the same app as the mobile client
STREAM_API_KEY=your-stream-api-key
STREAM_API_SECRET=your-stream-api-secret

# LLM: Gemini Realtime (voice teacher)
GOOGLE_API_KEY=your-google-api-key